// Miruro CloudStream Extension
version = 1

cloudstream {
    language = "en"
    description = "Anime via Miruro (AniList metadata + HLS streams). Sub & Dub supported. " +
        "Mirrors: miruro.tv / miruro.online / miruro.to / miruro.com / miruro.bz"
    authors = listOf("YourName")
    status = 1
    tvTypes = listOf("Anime", "AnimeMovie", "OVA")
    iconUrl = "https://www.miruro.tv/favicon.ico"
}
