// AniKoto CloudStream Extension
version = 1

cloudstream {
    language = "en"
    description = "Anime from AniKoto (anikototv.to). " +
        "Uses the public anikotoapi.site REST API + megaplay.buzz HLS streams. " +
        "Supports Sub & Dub. Same library as legacy HiAnime."
    authors = listOf("YourName")
    status = 1 // 1 = Working
    tvTypes = listOf("Anime", "AnimeMovie", "OVA")
    iconUrl = "https://anikototv.to/favicon.ico"
}
